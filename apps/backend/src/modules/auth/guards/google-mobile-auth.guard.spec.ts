import { GoogleMobileAuthGuard } from "./google-mobile-auth.guard";

describe("GoogleMobileAuthGuard", () => {
  it("should be defined", () => {
    expect(new GoogleMobileAuthGuard()).toBeDefined();
  });
});
